- install sublime as text editor
- install XAMPP as local server. https://www.apachefriends.org/index.html
  setup server tutorial https://www.maketecheasier.com/setup-local-web-server-all-platforms/
- React is the javascript library for building UI
- add javascript console in sublime, nodejs, find here  https://nodejs.org/en/
  Install nodejs issues, "DLL missing". Solution: cmd.exe, run as administrator, go to location of the MSI package and run msiexec /i packagename.msi. 
  When installed, 
  1. Download sublime text https://www.sublimetext.com/3 (it usually shows which one you need by black dot)
  2. Download node.js at https://nodejs.org/en/ (Recommended for most users)
  3. go to Tools > Build System > New Build System
  4. save as Javascript in the “User” directory inside Sublime Text settings folder

  {
    "cmd": ["C:/Program Files/nodejs/node.exe", "$file"],
    "selector": "source.js"
   }
   5.to check it, follow the last paragraph of this article from Time for fun part.